# ── Cloud backend (server) ──────────────────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.0
gunicorn>=22.0.0
eventlet>=0.35.0
onnxruntime>=1.17.0
numpy>=1.26.0
requests>=2.31.0
psutil>=5.9.0
pyahocorasick>=2.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# ── Windows agent only (do NOT install on Linux server) ────────────────────
# pydivert
# PySide6
# qdarktheme
# winotify
